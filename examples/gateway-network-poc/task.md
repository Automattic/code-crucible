# Gateway Network Routing Proof of Concept

Verify that fixture-backed container evaluation can route declared raw HTTP and HTTPS hostnames through the gateway sidecar when client code ignores proxy environment variables.
